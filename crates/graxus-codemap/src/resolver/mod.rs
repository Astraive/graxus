//! Resolvers for linking extracted facts to each other (imports to files, calls to symbols).

pub mod di_resolver;
pub mod import_resolver;
pub mod route_resolver;
pub mod symbol_resolver;
pub mod type_resolver;
