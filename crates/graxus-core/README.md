# graxus-core

Shared repository scanning, config loading, file classification, dependency discovery, and build-config parsing live here. This crate should stay small and reusable because the rest of the workspace depends on it.
